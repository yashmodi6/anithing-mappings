git add .
git commit -m "fix: update pre-commit script database queries

- Updated update_readme_stats.py to use LIKE queries against the new mappings JSON schema in sqlite instead of the deprecated schema columns"
git push
